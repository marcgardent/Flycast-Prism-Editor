# conda env create -f environment.yml
# conda env update -f environment.yml --prune

name: flycast-viewer
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - numpy
  - pillow
  - pip
  - pip:
    - customtkinter
    - platformdirs
    - openimageio
    - imageio