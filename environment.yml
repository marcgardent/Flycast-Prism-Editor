# conda env create -f environment.yml
# conda env update -f environment.yml --prune

name: flycast-viewer
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pillow
  - pip
  - pip:
    - customtkinter
    - briefcase
    - openimageio  # Meilleur support pour EXR multi-canaux
    - imageio
    - platformdirs